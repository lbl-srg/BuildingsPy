'''
This module contains the class *Tester* that runs
the unit tests of the `Buildings` library, and the 
class *Validator* that validates the html code of the info section
of the ``.mo`` files.
'''

